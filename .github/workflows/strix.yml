name: strix-cloud-scan
on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]
    tags: [ "v*" ]

jobs:
  strix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Install Strix
        run: curl -sSL https://strix.ai/install | bash
      - name: Strix Cloud Quick Scan
        env:
          STRIX_API_KEY: ${{ secrets.STRIX_API_KEY }}
        run: |
          export PATH="$HOME/.strix/bin:$PATH"
          strix cloud scans start --source . --yes --wait
          strix cloud vulns list --severity critical || true
